PORT=3000
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# From Telnyx Mission Control → Keys & Credentials → Public Key (base64, raw Ed25519)
TELNYX_PUBLIC_KEY=

# Local only — never in production
# SKIP_SIGNATURE_VERIFICATION=true

# Scripts only — never in production
VIRTUALSMS_API_KEY=